class Solution {
public:
    int missingNumber(vector<int>& nums) {
        int sum = 0;
        int x=nums.size();
        for(int i = 0; i<x; i++){
            sum += nums[i];    
        }
        return  (x+1)*(x)/2 - sum;
    }
};
